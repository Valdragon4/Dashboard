# Story 1.1: Architecture de Base des Connecteurs Bancaires

## Status

Done

## Story

**As a** développeur,
**I want** une architecture modulaire de connecteurs bancaires avec une classe abstraite commune,
**so that** je peux facilement ajouter de nouveaux providers sans dupliquer le code.

## Acceptance Criteria

1. Classe abstraite `BaseBankConnector` définie avec méthodes communes (`authenticate()`, `sync_transactions()`, `get_balance()`, `disconnect()`)
2. Interface standardisée pour les erreurs de connecteur (`BankConnectionError`, `AuthenticationError`, `RateLimitError`)
3. Structure de module `finance/connectors/` créée avec `__init__.py` et `base.py`
4. Documentation de l'architecture des connecteurs dans `docs/architecture/connectors.md`
5. Tests unitaires de base pour la classe abstraite

## Integration Verification

- IV1: Les imports existants CSV/PDF continuent de fonctionner sans modification
- IV2: Le code existant du scraper Trade Republic peut être intégré dans la nouvelle architecture
- IV3: Pas d'impact sur les performances du dashboard existant

## Tasks / Subtasks

- [x] Créer la structure de module `backend/finance/connectors/` (AC: 3)
  - [x] Créer le dossier `connectors/` dans `backend/finance/`
  - [x] Créer `__init__.py` avec exports appropriés
  - [x] Créer `base.py` pour la classe abstraite
- [x] Définir la classe abstraite `BaseBankConnector` dans `base.py` (AC: 1)
  - [x] Méthode abstraite `authenticate(credentials: dict) -> dict` pour l'authentification
  - [x] Méthode abstraite `sync_transactions(account, since: datetime) -> list[Transaction]` pour récupérer les transactions
  - [x] Méthode abstraite `get_balance(account) -> Decimal` pour récupérer le solde
  - [x] Méthode abstraite `disconnect()` pour fermer la connexion
  - [x] Méthode optionnelle `sync_portfolio_valuations(account) -> dict` pour les investissements (avec implémentation par défaut vide)
  - [x] Propriété `provider_name: str` abstraite pour identifier le provider
- [x] Créer les classes d'exception standardisées dans `base.py` (AC: 2)
  - [x] Classe `BankConnectionError(Exception)` comme exception de base
  - [x] Classe `AuthenticationError(BankConnectionError)` pour les erreurs d'authentification
  - [x] Classe `RateLimitError(BankConnectionError)` pour les erreurs de rate limiting
  - [x] Classe `ConnectionTimeoutError(BankConnectionError)` pour les timeouts
  - [x] Classe `InvalidCredentialsError(AuthenticationError)` pour credentials invalides
- [x] Documenter l'architecture dans `docs/architecture/connectors.md` (AC: 4)
  - [x] Expliquer le pattern de classe abstraite
  - [x] Documenter les méthodes requises et optionnelles
  - [x] Documenter les exceptions et leur utilisation
  - [x] Fournir un exemple de connecteur minimal
  - [x] Expliquer comment ajouter un nouveau connecteur
- [x] Créer les tests unitaires de base (AC: 5)
  - [x] Test que `BaseBankConnector` ne peut pas être instanciée directement
  - [x] Test qu'une classe héritant de `BaseBankConnector` doit implémenter toutes les méthodes abstraites
  - [x] Test des exceptions personnalisées
  - [x] Test de la méthode `sync_portfolio_valuations` par défaut (retourne dict vide)

## Dev Notes

### Previous Story Insights

Aucune story précédente - c'est la première story de l'epic.

### Architecture Context

[Source: `docs/brownfield-architecture.md`]

**Stack technique actuelle** :
- Framework : Django 4.0+, Python 3.x
- Database : PostgreSQL 16
- Task Queue : Celery 5.0+ avec Redis 7
- Patterns existants : Scraping Trade Republic via WebSocket + REST API avec authentification 2FA

**Structure du projet** :
```
backend/finance/
├── importers/          # Modules d'import existants
│   ├── loader.py       # Point d'entrée import CSV
│   ├── statement_csv.py
│   ├── traderepublic_csv.py
│   └── traderepublic_scraper.py  # Scraper Trade Republic existant
├── models.py           # Modèles Django (Account, Transaction, etc.)
├── views.py            # Vues Django
└── tasks.py            # Tâches Celery (si existantes)
```

**Note importante** : Il existe déjà un modèle `BankConnection` dans `models.py` (lignes 109-121) qui semble être une tentative précédente d'intégration avec POWENS. Ce modèle a des champs différents de ce qui est prévu dans le PRD. Pour cette story, nous créons uniquement l'architecture des connecteurs Python, pas les modèles Django (ce sera dans Story 1.2).

### Code Patterns and Conventions

[Source: `docs/brownfield-architecture.md#Code Patterns and Conventions`]

**Naming Conventions** :
- Connecteurs : `{Bank}Connector` (PascalCase)
- Méthodes de connecteur : `sync_transactions()`, `authenticate()`, `get_balance()` (snake_case)
- Modèles : PascalCase (`BankConnection`, `SyncLog`)

**Coding Standards** :
- Suivre les conventions Django existantes
- Utiliser les type hints Python pour la clarté
- Documentation docstring pour toutes les méthodes publiques
- Gestion d'erreurs explicite avec logging approprié

### File Locations

[Source: `docs/prd/technical-constraints-and-integration-requirements.md#Code Organization and Standards`]

**Nouveau module à créer** :
- `backend/finance/connectors/` - Module de connecteurs bancaires
  - `__init__.py` - Exports du module
  - `base.py` - Classe abstraite `BaseBankConnector` et exceptions

**Documentation à créer** :
- `docs/architecture/connectors.md` - Documentation de l'architecture des connecteurs

### Technical Constraints

[Source: `docs/brownfield-architecture.md`]

**Compatibilité requise** :
- Les imports CSV/PDF existants dans `finance/importers/loader.py` doivent continuer de fonctionner
- Le scraper Trade Republic existant (`finance/importers/traderepublic_scraper.py`) doit pouvoir être intégré dans la nouvelle architecture dans Story 1.3
- Pas d'impact sur les performances du dashboard existant

**Patterns existants à respecter** :
- Le scraper Trade Republic utilise une classe `TradeRepublicScraper` avec méthodes `initiate_login()`, `verify_2fa()`, `scrape_transactions()`
- Les imports utilisent des fonctions dans `loader.py` qui appellent des parsers spécialisés
- Les erreurs sont gérées avec des exceptions Python standard (`ValueError`, etc.)

### Data Models Reference

[Source: `backend/finance/models.py`]

**Modèles existants pertinents** :
- `Account` : Modèle de compte bancaire avec champs `owner`, `name`, `provider`, `type`, etc.
- `Transaction` : Modèle de transaction avec champs `account`, `posted_at`, `amount`, `description`, `raw` (JSONField)
- `BankConnection` : Modèle existant (lignes 109-121) avec champs différents du PRD - à noter pour Story 1.2

**Format de données Transaction** :
- `posted_at` : DateTimeField
- `amount` : DecimalField (positif pour revenus, négatif pour dépenses)
- `description` : CharField(max_length=255)
- `raw` : JSONField pour métadonnées

### API Specifications

[Source: `docs/brownfield-architecture.md#Integration Points`]

**Trade Republic API existante** (référence pour comprendre le pattern) :
- Endpoint auth : `https://api.traderepublic.com/api/v1/auth/web/login`
- WebSocket : `wss://api.traderepublic.com`
- Authentification : phone_number + PIN + 2FA code
- Token de session stocké dans cookie `tr_session`

**Pattern à suivre** :
- Les connecteurs doivent être indépendants de Django (classes Python pures)
- L'intégration avec Django se fera via les services dans Story 1.6

### Component Specifications

N/A - Cette story concerne uniquement l'architecture backend Python, pas de composants UI.

### Testing Requirements

[Source: `docs/brownfield-architecture.md#Testing Reality`]

**Tests existants** :
- Dossier `backend/finance/tests/` existe mais contenu non analysé
- Tests unitaires attendus avec Django TestCase

**Standards de test** :
- Utiliser `unittest.TestCase` ou `django.test.TestCase`
- Tests unitaires pour la classe abstraite
- Mocks pour les dépendances externes
- Tests que les méthodes abstraites doivent être implémentées

**Fichier de test à créer** :
- `backend/finance/connectors/tests/` (dossier)
- `backend/finance/connectors/tests/test_base.py` - Tests pour `BaseBankConnector`

### Technical Implementation Details

**Classe abstraite Python** :
- Utiliser `abc.ABC` et `@abstractmethod` de Python
- Type hints complets pour toutes les méthodes
- Docstrings détaillées expliquant le comportement attendu

**Structure de la classe abstraite** :
```python
from abc import ABC, abstractmethod
from datetime import datetime
from decimal import Decimal
from typing import Optional

class BaseBankConnector(ABC):
    """Classe abstraite de base pour tous les connecteurs bancaires."""
    
    @property
    @abstractmethod
    def provider_name(self) -> str:
        """Retourne le nom du provider bancaire."""
        pass
    
    @abstractmethod
    def authenticate(self, credentials: dict) -> dict:
        """Authentifie le connecteur avec les credentials fournis.
        
        Returns:
            dict: Informations de session (token, etc.)
        """
        pass
    
    @abstractmethod
    def sync_transactions(self, account, since: Optional[datetime] = None) -> list:
        """Récupère les transactions depuis la dernière synchronisation.
        
        Args:
            account: Objet Account Django
            since: Date optionnelle de la dernière synchronisation
            
        Returns:
            list: Liste de dictionnaires représentant les transactions
        """
        pass
    
    @abstractmethod
    def get_balance(self, account) -> Decimal:
        """Récupère le solde actuel du compte.
        
        Args:
            account: Objet Account Django
            
        Returns:
            Decimal: Solde du compte
        """
        pass
    
    @abstractmethod
    def disconnect(self) -> None:
        """Ferme la connexion et nettoie les ressources."""
        pass
    
    def sync_portfolio_valuations(self, account) -> dict:
        """Synchronise les valorisations de portefeuille (optionnel).
        
        Par défaut retourne un dict vide. À surcharger pour les providers
        supportant les investissements (Trade Republic, etc.).
        
        Returns:
            dict: Valorisations par type de portefeuille (PEA, CTO, etc.)
        """
        return {}
```

**Exceptions personnalisées** :
```python
class BankConnectionError(Exception):
    """Exception de base pour toutes les erreurs de connecteur bancaire."""
    pass

class AuthenticationError(BankConnectionError):
    """Erreur lors de l'authentification."""
    pass

class RateLimitError(BankConnectionError):
    """Erreur de rate limiting de l'API bancaire."""
    pass

class ConnectionTimeoutError(BankConnectionError):
    """Timeout lors de la connexion à l'API bancaire."""
    pass

class InvalidCredentialsError(AuthenticationError):
    """Credentials invalides fournis."""
    pass
```

### Project Structure Notes

[Source: `docs/prd/technical-constraints-and-integration-requirements.md#File Structure Approach`]

La structure proposée dans le PRD est :
```
backend/finance/
├── connectors/              # Nouveau module
│   ├── __init__.py
│   ├── base.py             # Classe abstraite
│   ├── traderepublic.py     # À créer dans Story 1.3
│   ├── boursorama.py       # À créer dans Story 1.4
│   └── hellobank.py         # À créer dans Story 1.5
```

Cette structure est cohérente avec la structure existante du projet qui utilise des modules séparés (`importers/`).

### Testing

**Test file location** :
- `backend/finance/connectors/tests/test_base.py`

**Testing frameworks** :
- Django TestCase (`django.test.TestCase`)
- unittest.mock pour mocker les méthodes abstraites

**Test standards** :
- Tests unitaires pour chaque composant
- Utiliser des mocks pour tester les classes abstraites
- Vérifier que les méthodes abstraites doivent être implémentées

**Key test scenarios** :
1. Test qu'on ne peut pas instancier `BaseBankConnector` directement
2. Test qu'une classe héritant doit implémenter toutes les méthodes abstraites
3. Test que `sync_portfolio_valuations` retourne un dict vide par défaut
4. Test que les exceptions personnalisées héritent correctement
5. Test que les exceptions peuvent être attrapées par leur classe parente

**Example test structure** :
```python
from django.test import TestCase
from finance.connectors.base import (
    BaseBankConnector,
    BankConnectionError,
    AuthenticationError,
    RateLimitError
)

class TestBaseBankConnector(TestCase):
    def test_cannot_instantiate_abstract_class(self):
        """Test qu'on ne peut pas instancier BaseBankConnector directement."""
        with self.assertRaises(TypeError):
            BaseBankConnector()
    
    def test_must_implement_abstract_methods(self):
        """Test qu'une classe héritant doit implémenter toutes les méthodes abstraites."""
        class IncompleteConnector(BaseBankConnector):
            pass
        
        with self.assertRaises(TypeError):
            IncompleteConnector()
```

## Change Log

| Date | Version | Description | Author |
| ---- | ------- | ----------- | ------ |
| 2025-01-XX | 1.0 | Création initiale de la story | SM |
| 2025-01-XX | 1.1 | Implémentation complète de la story - Architecture de base créée, tests passent | Dev |

## Dev Agent Record

### Agent Model Used

Composer (Cursor AI)

### Debug Log References

Aucun debug log nécessaire - implémentation directe sans erreurs.

### Completion Notes List

- **Structure de module créée** : Le module `backend/finance/connectors/` a été créé avec la structure complète incluant le dossier `tests/`.
- **Classe abstraite implémentée** : `BaseBankConnector` avec toutes les méthodes abstraites requises, incluant la propriété `provider_name` et la méthode optionnelle `sync_portfolio_valuations`.
- **Exceptions standardisées** : Toutes les exceptions ont été créées avec la hiérarchie correcte (BankConnectionError → AuthenticationError → InvalidCredentialsError, etc.).
- **Documentation complète** : Documentation détaillée créée dans `docs/architecture/connectors.md` avec exemples et guide pour ajouter de nouveaux connecteurs.
- **Tests unitaires** : 12 tests créés couvrant tous les scénarios requis. Tous les tests passent avec succès.
- **Compatibilité** : Aucun impact sur le code existant - les imports CSV/PDF continuent de fonctionner normalement.
- **Standards respectés** : Code conforme aux conventions Django, avec type hints complets et docstrings détaillées.

### File List

**Fichiers créés** :
- `backend/finance/connectors/__init__.py` - Module d'export des connecteurs
- `backend/finance/connectors/base.py` - Classe abstraite BaseBankConnector et exceptions
- `backend/finance/connectors/tests/__init__.py` - Module de tests
- `backend/finance/connectors/tests/test_base.py` - Tests unitaires pour BaseBankConnector
- `docs/architecture/connectors.md` - Documentation de l'architecture des connecteurs

**Fichiers modifiés** :
- Aucun fichier existant modifié (nouveau module créé)

## QA Results

### Review Date: 2025-01-XX

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Excellent** - L'implémentation de l'architecture de base des connecteurs bancaires est de haute qualité. Le code est bien structuré, suit les meilleures pratiques Python/Django, et fournit une base solide pour les connecteurs futurs.

**Points forts** :
- Architecture modulaire claire avec pattern de classe abstraite bien implémenté
- Hiérarchie d'exceptions complète et logique pour gestion d'erreurs robuste
- Documentation exhaustive avec exemples pratiques
- Tests unitaires complets couvrant tous les scénarios requis
- Type hints complets pour meilleure maintenabilité
- Docstrings détaillées pour toutes les méthodes publiques

**Aucune dette technique identifiée** - Le code est prêt pour les stories suivantes.

### Refactoring Performed

Aucun refactoring nécessaire - le code est déjà bien structuré et suit les standards du projet.

### Compliance Check

- **Coding Standards**: ✓ Conforme aux conventions Django et Python
- **Project Structure**: ✓ Fichiers créés aux emplacements corrects selon le PRD
- **Testing Strategy**: ✓ Tests unitaires complets avec Django TestCase
- **All ACs Met**: ✓ Tous les critères d'acceptation sont satisfaits

### Requirements Traceability

**AC1 - Classe abstraite BaseBankConnector** : ✓
- Classe abstraite créée avec toutes les méthodes requises
- Tests : `test_cannot_instantiate_abstract_class`, `test_must_implement_abstract_methods`, `test_complete_implementation_can_be_instantiated`

**AC2 - Interface standardisée pour les erreurs** : ✓
- 5 exceptions créées avec hiérarchie correcte
- Tests : `TestExceptionHierarchy` avec 6 tests couvrant toutes les relations d'héritage

**AC3 - Structure de module** : ✓
- Module `finance/connectors/` créé avec `__init__.py` et `base.py`
- Structure conforme au PRD

**AC4 - Documentation architecture** : ✓
- Documentation complète dans `docs/architecture/connectors.md`
- Inclut exemples, guide d'ajout de nouveaux connecteurs, et bonnes pratiques

**AC5 - Tests unitaires de base** : ✓
- 12 tests créés couvrant tous les scénarios requis
- Tous les tests passent avec succès

### Test Architecture Assessment

**Couverture** : Excellente
- 12 tests unitaires couvrant tous les aspects de la classe abstraite et des exceptions
- Tests bien organisés en deux classes de test (`TestBaseBankConnector`, `TestExceptionHierarchy`)
- Tests vérifient les comportements attendus et les contraintes d'implémentation

**Qualité des tests** :
- Tests clairs avec docstrings explicatives
- Utilisation appropriée de `assertRaises` pour vérifier les contraintes abstraites
- Tests de la hiérarchie d'exceptions avec vérification de l'héritage multiple
- Test de la méthode optionnelle `sync_portfolio_valuations` avec valeur par défaut

**Niveau de test approprié** : Unitaires uniquement (approprié pour cette story d'architecture)

### Security Review

**Status** : ✓ PASS

**Findings** :
- Aucune vulnérabilité de sécurité identifiée
- Pas de secrets hardcodés
- Exceptions appropriées pour gestion sécurisée des erreurs d'authentification
- Architecture de base ne gère pas directement les credentials (sera dans Story 1.2)

**Recommandations** :
- Aucune action immédiate requise
- La gestion sécurisée des credentials sera évaluée dans Story 1.2

### Performance Considerations

**Status** : ✓ PASS

**Findings** :
- Architecture légère avec classes abstraites Python standard (pas de surcharge)
- Aucun impact sur les performances du dashboard existant (IV3 vérifié)
- Pas de dépendances externes lourdes ajoutées

**Recommandations** :
- Aucune optimisation nécessaire à ce stade
- Les performances des connecteurs concrets seront évaluées dans les stories suivantes (1.3-1.5)

### Integration Verification

**IV1 - Imports CSV/PDF existants** : ✓ Vérifié
- Aucun fichier existant modifié
- Nouveau module créé sans impact sur le code existant

**IV2 - Scraper Trade Republic** : ✓ Vérifié
- Architecture conçue pour faciliter l'intégration du scraper existant
- Pattern compatible avec la classe `TradeRepublicScraper` existante

**IV3 - Performances dashboard** : ✓ Vérifié
- Aucun impact sur les performances (module non utilisé encore)
- Architecture légère sans surcharge

### Improvements Checklist

- [x] Architecture de base complète et bien structurée
- [x] Tests unitaires complets et passants
- [x] Documentation exhaustive avec exemples
- [x] Hiérarchie d'exceptions complète
- [ ] Considérer l'ajout de logging structuré dans les méthodes abstraites (amélioration future)
- [ ] Évaluer l'ajout de méthodes utilitaires communes dans BaseBankConnector (amélioration future)

### Files Modified During Review

Aucun fichier modifié pendant la review - le code est déjà de haute qualité.

### Gate Status

**Gate**: PASS → `docs/qa/gates/1.1-architecture-base-connecteurs-bancaires.yml`

**Quality Score**: 100/100

**Risk Profile**: Low - Architecture de base sans risques identifiés

**NFR Assessment**: Tous les NFRs passent (Security, Performance, Reliability, Maintainability)

### Recommended Status

✓ **Ready for Done** - La story est complète, bien testée, et documentée. Aucun changement requis avant de marquer comme "Done".
